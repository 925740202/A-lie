#include<iostream>
#include<stdio.h>
#include<map>
#include<string>
using namespace std;
const int N=1048577;
int main()
{
    map<string,int> voc;   //定义map用来存放出现最多的单词和出现次数
    string sen;
    cin.getline(sen,N)//输入所要处理的句子
    string word;
    for(string::iterator it=sen.begin();it!=sen.end();it++)
    {

        if(voc.find)
    }
	return 0;
}

